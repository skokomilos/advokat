<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.berka.advokatormlite">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.PronadjeniSlucaj"/>
        <activity android:name=".activities.NadjiSlucajActivity"/>
        <activity android:name=".activities.PostupakListActivity"/>
        <activity android:name=".activities.AddNewCaseActivity"/>
        <activity android:name=".activities.KonacniTrosakSvihRadnjiActivity"/>
        <activity android:name=".activities.ChoosePostupakActivity"/>
        <activity android:name=".activities.AddParnicaActivity"/>
        <activity android:name=".activities.AddKrivicaActivity"/>
        <activity android:name=".activities.AddPrekrsajniActivity"/>
        <activity android:name=".activities.AddUstavniActivity"/>

    </application>

</manifest>