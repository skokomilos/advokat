<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.berka.advokatormlite">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/trf_logo"
        android:label="@string/app_name"
        android:name=".dependencyinjection.App"
        android:roundIcon="@drawable/trf_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".activities.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.add_points.PronadjeniSlucajActivity1"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".activities.konacni.KonacniTrosakSvihRadnjiActivity"/>
        <activity android:name=".activities.parnica.AddParnicaActivity"/>
        <activity android:name=".activities.add_case.AddSlucaj"/>
        <activity android:name=".activities.prekrsaj.AddPrekrsajniActivity"/>
        <activity android:name=".activities.ustavni.AddUstavniActivity"/>
        <activity android:name=".activities.IspraveActivity"/>

    </application>

</manifest>